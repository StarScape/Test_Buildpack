#!bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Found build.py"

if [ ! -s $1/hello.txt ]; then
  echo "build.py is empty" | indent
  exit 1
fi

echo "Hello from the world of buildpacks!" > buildpack_magic.txt